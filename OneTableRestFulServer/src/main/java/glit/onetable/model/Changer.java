package glit.onetable.model;

public class Changer {
	/*
	public ArrayList<Ingredient> ingredientChanger(ArrayList<AnalyzeVariety> avList) {
		ArrayList<Ingredient> il = new ArrayList<Ingredient>();
		Ingredient ingredient = new Ingredient();

		for(int i=0; i<avList.size(); i++) {
			ingredient.setDisplayName(avList.get(i).getDisplayName());
			ingredient.setImgUrl(avList.get(i).getImg());
			ingredient.setIngredientItemId(avList.get(i).getItemId());
			ingredient.setUnitAmount(avList.get(i).getUnitNum());
			il.add(ingredient);
		}
		return il;

	}

	public ArrayList<IngredientPrice> priceChanger(ArrayList<AnalyzeVariety> avList) {
		ArrayList<IngredientPrice> ip = new ArrayList<IngredientPrice>();
		IngredientPrice iPrice = new IngredientPrice();
		for(int i=0; i<avList.size(); i++) {
			iPrice.setPrice(avList.get(i).getPrice());
			iPrice.setIngredientItemId(avList.get(i).getItemId());
			ip.add(iPrice);
		}
		return ip;

	}

*/

}
