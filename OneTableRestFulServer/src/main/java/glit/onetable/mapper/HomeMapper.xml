<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="glit.onetable.mapper.HomeMapper">

	<select id="popularRecipe" resultType="Recipe">
		SELECT recipeIdx, userIdx, name, price, recipeImg, nickName, recipeDate FROM onetable.recipe_unit order by queryCnt desc, name limit 0, 30
	</select>
</mapper>